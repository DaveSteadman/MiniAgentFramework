I need to produce a report of the latest AI news headlines.

Step 1 - Search KoreData feeds:
- Run koredata_search multiple times with different targeted queries to build a pool of candidate articles. Use limit=20 for each call and restrict to domains=["feeds"].
- Required queries (run all of them, do not skip any):
    "artificial intelligence"
    "machine learning"
    "large language model"
    "OpenAI OR Anthropic OR Google DeepMind"
    "AI regulation OR AI safety OR AI ethics"
    "neural network OR generative AI"
- Set since to 7 days before today's date on every call so only recent articles are returned.
- After all queries, de-duplicate by entry ID and keep a combined pool.
- For each candidate in the pool: call koredata_get_entry to fetch the full article text and save it to the scratchpad. Discard entries with fewer than 200 words of body text - they are not substantive enough to write a section from.
- You must have the full entry text in the scratchpad before writing any section. Never write a section based only on a title or snippet - that produces fabricated content.

Step 2 - Supplement with web sources if needed:
- If the KoreData pool has fewer than 8 substantive articles after Step 1, read ai-sites.json ("ai-sites.json", no leading "./" prefix) and work through the listed URLs.
- For each source URL, fetch the landing page, extract article links, and drill down up to 3 layers deep to reach individual article pages.
- Fetch each article page and save content to the scratchpad. Discard if fewer than 200 words or not AI-relevant.
- If still short after exhausting ai-sites.json, supplement with search_web. If search_web fails, retry with search_web_text.
- Do not use an explicit ISO date string in the web search query - DuckDuckGo cannot handle date strings like 2026-04-04.

Step 3 - Write the report:
- Select the 8-10 best de-duplicated stories from the combined pool.
- Write each as a flowing news summary paragraph using only facts present in the fetched article text. Do not invent details, quotes, statistics, or events not found in the source.
- If you cannot reach 8 sections from real fetched content, write fewer sections rather than fabricating topics to fill the quota. State at the end of the report how many sources were used.
- Each section must be a minimum of 400 words. Target approximately 500 words per section. Do not use bullet lists.
- Number sections sequentially (1, 2, 3 ...) with no duplicated numbers.

Notes:
- Wikipedia is an encyclopedia reference source. It does not contain news headlines and is prohibited for this task.
- File paths passed to read_file and write_file must NOT start with "./". Use bare relative paths like "2026-04-05/ai-headlines.md".

Output:
- Create the output folder first with create_folder if needed, then write the report to a markdown file at data/{todays date}/ai-headlines.md.

Housekeeping:
- After writing the report, save an updated version of ai-sites.json with successful sites at the top and unsuccessful ones at the bottom. If a web search highlighted a new source, add it to the list.







